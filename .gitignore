node_modules/
.env
.env.local
.env*.local
.next/
dist/
coverage/
uploads/
*.log
.DS_Store
.vercel/
# datos personales NUNCA al repo — en cualquier nivel del monorepo
**/data/private/
# salidas de la evaluación de Coachy (contienen texto generado sobre un atleta)
apps/web/eval/*
!apps/web/eval/README.md

# next / build
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
*.tsbuildinfo

# prisma
prisma/*.db

# test output
.vitest/
.vercel
# Videos libres preparados para subir a Storage — pesan 111 MB, no van al repo
videos-libres/

# paquete de validacion con expertos (Fase 5): perfiles reales de atletas,
# nunca al repo. El script que los genera si va (apps/web/scripts/paquete-validacion.ts).
/validacion/*.md
/validacion/*.pdf
!/validacion/README.md
